<!DOCTYPE html>
<html>
<head>
    <title></title>
</head>
<body>
    <div id='main_wrapper'>
        <h1>Listing Products</h1>
        <table>
            <tr>
                <th>Name</th>
                <th>Price</th>
                <th>Description</th>
                <th>Category</th>
            </tr>
            <% @products.each do |p| %>
                <tr>
                    <td><%= p.name %></td>
                    <td><%= p.price %></td>
                    <td><%= p.description %></td>
                    <td><%= p.category.name %></td>
                    <td>
                        <a href='/products/<%= p.id %>'>Show</a>   |
                        <a href='/products/<%= p.id %>/edit'>Edit</a>   |
                        <a href= '/products/<%= p.id %>' data-method='delete'>Destroy</a>
                    </td>
                </tr>
            <% end %>

        </table>

        <a href='/products/new'>New Product</a>

    </div>
</body>


</html>
